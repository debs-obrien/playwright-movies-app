create a test for each of the points in section 1 and save each test in its own test file. use list utilitie functions to avoid duplicating steps when possible

Add an 'agent' tag to the describe block of each test file.
